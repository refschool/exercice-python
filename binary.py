"""def findBinary(decimal):
    if(decimal == 0 ):
        return 0
    else:
        return decimal % 2 + 10 * (findBinary(decimal // 2))


print(findBinary(2))


variables
boucle : for ,  while
fonction y compris recursive
les objets et les classes
paquet + modules
représentation de données : liste,



3 heures python : pyzo
54, 61,  2

tri par insertion, par sélection, recherche dichotomique,
algo de tri,"""

pdf = "david-bustamante-cruz-php.pdf"
"""
in_file = os.path.abspath(sys.argv[1])  
out_file = os.path.abspath(sys.argv[2])
"""
suffixe = pdf.split('.')[0].split('-').pop()
print(suffixe)